import express from "express"

const router = express.Router()

//Criar um item
router.post("/items", async (req,res) => {
    try {
        const data = 
    } catch (error) {
        
    }

}

)


//Pegar todos os items
router.get(


)

//Pegar um item por id

router.get(


)

//Alterar um item por id

router.put(


)

// Alterar informação especifica de um item por id

router.patch(


)

router.delete(


)

export default router;